package com.djekgrif.weather.core.domain.settings

/** App-wide temperature unit preference. Conversion to display values happens in presentation. */
enum class TemperatureUnit {
    CELSIUS,
    FAHRENHEIT,
}
